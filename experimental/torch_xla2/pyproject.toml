[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
version = "0.0.1"
name = "torch_xla2"
dependencies = [
    "torch>=2.1",
    "jax>=0.4.24",
    "jaxlib",
]

requires-python = ">=3.10"
license = {file = "LICENSE"}
